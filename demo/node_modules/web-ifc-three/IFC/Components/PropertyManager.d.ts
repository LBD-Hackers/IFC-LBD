import { IfcState } from '../BaseDefinitions';
import { BufferGeometry } from 'three';
/**
 * Contains the logic to get the properties of the items within an IFC model.
 */
export declare class PropertyManager {
    private state;
    constructor(state: IfcState);
    getExpressId(geometry: BufferGeometry, faceIndex: number): number | undefined;
    getItemProperties(modelID: number, id: number, recursive?: boolean): any;
    getAllItemsOfType(modelID: number, type: number, verbose: boolean): any[];
    getPropertySets(modelID: number, elementID: number, recursive?: boolean): any[];
    getTypeProperties(modelID: number, elementID: number, recursive?: boolean): any[];
    getSpatialStructure(modelID: number): {
        expressID: number;
        type: string;
        children: never[];
    };
    private newIfcProject;
    private getSpatialTreeChunks;
    private getChunks;
    private getSpatialNode;
    private getChildren;
    private newNode;
    private getAllRelatedItemsOfType;
    private getRelated;
    private isRelated;
}
