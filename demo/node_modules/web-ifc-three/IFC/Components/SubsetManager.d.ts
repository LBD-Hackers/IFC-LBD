import { BufferGeometry, Material, Mesh, Scene } from 'three';
import { IfcState, HighlightConfigOfModel } from '../BaseDefinitions';
/**
 * Contains the logic to get, create and delete geometric subsets of an IFC model. For example,
 * this can extract all the items in a specific IfcBuildingStorey and create a new Mesh.
 */
export declare class SubsetManager {
    private state;
    private selected;
    constructor(state: IfcState);
    getSubset(modelID: number, material?: Material): Mesh<BufferGeometry, Material | Material[]> | null;
    removeSubset(modelID: number, scene?: Scene, material?: Material): void;
    createSubset(config: HighlightConfigOfModel): void | Mesh<BufferGeometry, Material | Material[]>;
    private createSelectionInScene;
    private isConfigValid;
    private isValid;
    private getGeomAndMat;
    private updatePreviousSelection;
    private newSelectionGroup;
    private isPreviousSelection;
    private containsIds;
    private addToPreviousSelection;
    private filter;
    private filterGeometries;
    private isEasySelection;
    private matID;
    private matIDNoConfig;
}
