import { IFragment, SubsetConfig } from '../base-types';
/**
 * Contains the logic to get, create and delete geometric subsets of an IFC model. For example,
 * this can extract all the items in a specific IfcBuildingStorey and create a new Mesh.
 */
export declare class SubsetManager {
    private fragment;
    private items;
    private tempIndex;
    private ids;
    constructor(fragment: IFragment);
    createSubset(config: SubsetConfig): void;
    removeFromSubset(fragment: IFragment, ids: number[]): void;
    dispose(): void;
    private createDefaultSubset;
    private initializeSubsetGroups;
    private filterIndices;
    private constructSubsetByMaterial;
    private insertNewIndices;
    private insertIndicesAtGroup;
    private getCurrentGroup;
    private resetGroups;
}
