import { ItemsMap } from './items-map';
import { SubsetConfig, Subsets } from '../base-types';
export declare class SubsetCreator {
    private items;
    private subsets;
    private tempIndex;
    constructor(items: ItemsMap, subsets: Subsets);
    createSubset(config: SubsetConfig, subsetID: string): import("../fragment-mesh").FragmentMesh;
    dispose(): void;
    private initializeSubset;
    private initializeSubsetGroups;
    private filterIndices;
    private constructSubsetByMaterial;
    private insertNewIndices;
    private insertIndicesAtGroup;
    private getCurrentGroup;
    private resetGroups;
}
